run: true
version: '3-dev.28'
description: |
  Dev 1:
  - Full engine rewrite:
    - Pipeline: tokenizer -> parser -> AST -> evaluator
    - Nested tokens and hierarchical identifiers now fully supported
    - Regex system removed; resolution is deterministic and syntax‑scoped.
  Dev 2:
  - Clean up
  - Bug fixes
  Dev 3:
  - Fixed AST
  Dev 4:
  - Import Node
  Dev 5:
  - Updated changelog format
  - Fix init
  Dev 6:
  - Fix
  Dev 7:
  - Fixed tokenizer logic
  Dev 8:
  - Fixed parser
  Dev 9:
  - Fixed datamaps
  - Added "callable" mode
  - Fixed logic
  Dev 10:
  - Simplified comments
  Dev 11-12:
  - Patched tokenizer to correctly allow escapes
  Dev 13-14:
  - Datamap supports both class and instance
  Dev 15:
  - Grouped tokenizer rewrite (paired/toggle/mixed)
  - Correct IDENT grammar + delimiter state fixes
  - Nested class datamaps (hierarchical maps)
  - MissingKeyError + per‑map missing_key modes
  - Recursion moved to evaluator (uniform + syntax‑scoped)
  Dev 16:
  - Added ignore_delimiters paramater to datamap
    - Updated evaulator logic to apply
  Dev 17:
  - Fixed critical logic bug
  Dev 18-20:
  - Debugging
  Dev 21:
  - Remove recursion and some paramaters temporarily
  Dev 22:
  - Made parser and tokenizer much more strict
  Dev 23:
  - Readded $(IDENT) pattern
  Dev 24:
  - Readded recursion
  - Added max_depth to datamap decorator
  - Fixed cycle detection
  Dev 25:
  - Fixed datamap's SyntaxFactory
  Dev 26:
  - Created utils.py
  - Added rprint() for simple global resolution and print
  Dev 27:
  - Removed LPAREN and RPAREN tokens from the tokenizer, fixing a bug in the parser that caused it to fail on nested calls
  Dev 28:
  - Fixed tokenizer to correctly handle nested calls and emit the correct tokens for IDENT, LPAREN, TEXT, and RPAREN
